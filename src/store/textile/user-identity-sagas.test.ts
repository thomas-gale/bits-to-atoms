import { generateIdentity } from './user-identity-sagas';

describe('generate identity', () => {
  it('should run', async () => {
    // Arrange
    // Act
    await generateIdentity();
    // Assert
  });
});
